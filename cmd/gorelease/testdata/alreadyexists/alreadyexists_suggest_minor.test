mod=example.com/basic
base=v0.0.1
success=false
# B() was added, so now it should suggest a new minor version. But, it should
# not suggest v0.1.0, which already exists: it should suggest the next highest
# minor version not previously suggested.
-- want --
Suggested version v0.2.0
-- go.mod --
module example.com/basic

go 1.12
-- a/a.go --
package a

func A() int { return 0 }
func B() int { return 0 }
