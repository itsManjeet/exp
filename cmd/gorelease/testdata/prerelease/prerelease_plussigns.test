mod=example.com/prerelease
release=v1.0.0-alpha+001
-- want --
release version "v1.0.0-alpha+001" is not a canonical semantic version: build
metadata is not supported by the Go command
-- go.mod --
module example.com/prerelease

go 1.12